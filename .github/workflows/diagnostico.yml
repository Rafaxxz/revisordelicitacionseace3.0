name: Diagnóstico de conexión SEACE/OECE
on:
  workflow_dispatch:
  push:
    paths: [".github/workflows/diagnostico.yml", "scripts/diagnostico.sh"]
jobs:
  probar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/diagnostico.sh
